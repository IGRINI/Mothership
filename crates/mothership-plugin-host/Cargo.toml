[package]
name = "mothership-plugin-host"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Pinned to 37 (MSRV 1.87, compatible with the workspace's rustc 1.90) — this is
# also the first release with the WASI 0.3 native-async preview. Bumping past 44
# requires rustc >= 1.93.
wasmtime = "37"
wasmtime-wasi = "37"
anyhow.workspace = true
